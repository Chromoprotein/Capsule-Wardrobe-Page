export default function clothingPlaceholder(id, category) {
    const placeholder = "";
    if(category === "dress") {
        placeholder = "";
    }
    else if(category === "skirt") {
        placeholder = "";
    } 
    else if (category === "shirt") {
        placeholder = "";
    }
    else if (category === "sweater") 
}